# CI Leakage Scanner Allowlist — local
# Lines starting with # are comments.
# Entry = filename (skips entire file) or substring (skips matching lines).

# CPC setup documentation — legitimately references CPC paths, Volumes, env vars
per_machine_setup.md

# Skill documentation and hooks use CPC install paths in examples
C:\CPC\
C:\\CPC\\

# Hook files contain legitimate CPC references in error message examples
breadcrumb_start_guard.js
