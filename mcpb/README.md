# MCPB bundle source

This folder contains the bundle manifest used to package local as a `.mcpb` file for one-click install in Claude Desktop and other MCP clients.

The actual `.mcpb` file is built by GitHub Actions on release (or locally with `mcpb pack`).

Spec: https://github.com/modelcontextprotocol/mcpb
